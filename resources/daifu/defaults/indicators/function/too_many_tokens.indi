{:id    :too-many-tokens
 :type  :function
 :doc   "returns true if a function has too many tokens"
 :level :warn

 :default {:limit 40}
 :arglist [:token-count]
 :source (fn [tokens _ options]
            (> tokens (:limit options)))}
